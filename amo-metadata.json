{
  "categories": {
    "firefox": ["web-development"]
  },
  "summary": {
    "en-US": "Surfaces Treeherder try push status on Phabricator and Bugzilla pages."
  },
  "description": {
    "en-US": "<p>Adds a <strong>Try Pushes</strong> panel above the Details section on Phabricator D* pages and above the comments on Bugzilla bug pages. For each try push associated with the revision or bug it shows:</p><ul><li>Timestamp</li><li>Short revision hash (links to Treeherder)</li><li>Build / Lint / Tests status badges</li><li>Job count summary (completed, failed, running, pending)</li></ul><p>The panel refreshes automatically every 60 seconds while any push has jobs still running.</p><h3>How it works</h3><ol><li><strong>Page detection.</strong> When you open a Phabricator D* page or a Bugzilla show_bug.cgi page, a content script extracts the D-number or bug number from the URL and the author from the page DOM.</li><li><strong>Try push search.</strong> A background worker fetches up to 500 try pushes for the revision author via the Treeherder API and filters them by D-number or bug number in each push&#39;s commit message. For pushes made with <code>mach try auto</code> (which does not write the D-number into the commit message), the extension falls back to walking one level up the Mercurial commit graph via hg.mozilla.org: the parent of the try tip commit is the user&#39;s patch commit, which carries the Differential Revision trailer.</li><li><strong>Health enrichment.</strong> For each matched push the background fetches its build/lint/test health summary from Treeherder.</li><li><strong>Panel render.</strong> The enriched push list is inserted into the page. Results are cached for two minutes; the cache is bypassed on manual reload.</li><li><strong>Auto-refresh.</strong> If any push has jobs still running, a 60-second interval fetches fresh data and updates the panel in place, stopping automatically once all jobs finish.</li></ol><p>You can optionally set your Treeherder / Bugzilla email address in the extension preferences to speed up the author search for older pushes.</p>"
  },
  "version": {
    "license": "MPL-2.0"
  }
}
